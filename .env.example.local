# ==============================================
# Local/Mock Data Configuration (No Database)
# ==============================================
# Use this configuration for local testing with mock data
# No MongoDB connection required - uses in-memory mock data
# Copy this file to .env for offline/local development

# Server Configuration
PORT=3000
NODE_ENV=development

# MongoDB Configuration
# Leave MONGODB_URI empty or commented out to use mock data mode
# MONGODB_URI=

# JWT Secret Key
# IMPORTANT: Generate a secure random key for production
# You can generate one using: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET=dev-secret-key-for-local-testing-only
# Or use:
# JWT_SECRET_KEY=dev-secret-key-for-local-testing-only

#Client URL for CORS
CLIENT_URL=http://localhost:3001

# API Configuration
API_VERSION=v1
MAX_REQUEST_SIZE=10mb

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=10000

# Walking Speed (km/h)
DEFAULT_WALKING_SPEED=5

# Route Deviation Threshold (meters)
ROUTE_DEVIATION_THRESHOLD=50

#Bcrypt Salt Rounds
BCRYPT_SALT_ROUNDS=10

#Mock Admin Password
MOCK_ADMIN_PASSWORD = 'Password123!'
# ==============================================
# Mock Data Mode Features:
# ==============================================
# - All data stored in memory (api/data/mockData.js)
# - Changes reset when server restarts
# - No database connection required
# - Perfect for testing and development
# - Mock users (userId 1, 2, 3) have admin role
#
# Mock Admin Users:
# - John Smith (userId: 1, email: john.smith@example.com)
# - Maria Garcia (userId: 2, email: maria.garcia@example.com)
# - Chen Wei (userId: 3, email: chen.wei@example.com)
#
# ==============================================
# Setup Instructions:
# ==============================================
# 1. Copy this file: cp .env.example.local .env
# 2. (Optional) Update JWT_SECRET if needed
# 3. Start server: npm start
# 4. Server will automatically use mock data mode
# 5. Check console for "Using mock data mode" message
